/* eslint-disable prettier/prettier */
import { userType } from "./cuentas";

export type volanteCalidadType = {
    tipoFruta: string,
    unidades: number,
    defectos: number,
    fecha: string,
    operario: userType,
}
