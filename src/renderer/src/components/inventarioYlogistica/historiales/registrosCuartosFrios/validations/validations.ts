/* eslint-disable prettier/prettier */
export type formType = {
    _id: string,
    action: string,
    createdAt: string,
    documentId: {
        nombre: string,
    }
    operation: string,
    user: string,
}
